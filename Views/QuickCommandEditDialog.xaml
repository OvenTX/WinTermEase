<Window x:Class="WindowsTerminal.Views.QuickCommandEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="编辑快捷指令" Width="380" Height="360"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanMinimize"
        Background="#252526">

    <Window.Resources>
        <Style TargetType="TextBlock" BasedOn="{StaticResource DarkLabel}"/>
        <Style TargetType="TextBox" BasedOn="{StaticResource DarkTextBox}"/>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="名称" Margin="0,0,0,4"/>
            <TextBox x:Name="TxtName"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="指令内容" Margin="0,0,0,4"/>
            <TextBox x:Name="TxtCommand" Height="60" TextWrapping="Wrap"
                     AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock Text="分组" Margin="0,0,0,4"/>
            <TextBox x:Name="TxtGroup" Text="Default"/>
        </StackPanel>

        <CheckBox Grid.Row="3" x:Name="ChkNewLine" Content="发送后自动换行 (\n)"
                  Foreground="#F0F0F0" IsChecked="True" Margin="0,0,0,10"/>

        <DockPanel Grid.Row="5" Margin="0,10,0,0">
            <Button Content="取消" DockPanel.Dock="Right" Width="80"
                    Style="{StaticResource DarkButton}" Click="BtnCancel_Click"/>
            <Button Content="保存" DockPanel.Dock="Right" Width="80" Margin="0,0,8,0"
                    Style="{StaticResource AccentButton}" Click="BtnSave_Click"/>
        </DockPanel>
    </Grid>
</Window>
